import AutoHeader from '$store/components/autoheader/index.tsx';
import type { Props } from '$store/components/autoheader/index.tsx';

function AutoHeaderSection(props: Props) {
    return <AutoHeader {...props} />;
}

export default AutoHeaderSection;